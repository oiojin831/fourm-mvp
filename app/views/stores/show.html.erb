<% @media.each do |medium| %>
  <div class='box'>
    <div class='content'>
      <%= image_tag medium.standard_resolution %>
    </div>
    <div class='info'>
      <span class='price'> $<%= medium.tag.price %>
      <span class='name'> <%= medium.tag.name %>
    </div>
  </div>
<% end %>
